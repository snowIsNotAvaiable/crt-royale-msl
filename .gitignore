# macOS
.DS_Store

# Xcode
build/
*.xcuserdata
DerivedData/

# Metal compiled
*.air
*.metallib

# Documentation (privat -- Bericht, Status, Phase-4-Evaluation etc.)
docs/

# Python-venv (Setup: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt)
.venv/

# Swift Package build artifacts (regenerable via tests/validate.sh)
.build/
.swiftpm/
Package.resolved

# Validation pipeline runtime artifacts (regenerable):
#   - inputs/*.png are produced by tests/inputs/generate.py
#   - outputs/   is produced by tests/validate.sh
#   - reference/ is produced by tests/capture_reference.sh (librashader-cli)
#   - diff/      is produced by tests/compare_all.sh
tests/inputs/*.png
tests/outputs/
tests/reference/
tests/diff/
